import { FollowerStatusIconPipe } from './follower-status-icon.pipe';

describe('FollowerStatusIconPipe', () => {
  it('create an instance', () => {
    const pipe = new FollowerStatusIconPipe();
    expect(pipe).toBeTruthy();
  });
});
